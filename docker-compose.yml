version: "3.8"

services:
  volumetry-agent:
    build:
      context: .
    container_name: volumetry-agent
    ports:
      - "8899:8000"
    volumes:
      - ./:/app
      - /tmp:/tmp
      - medical-app_studies-data:/app/storage/studies:consistent
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - holonauts-network

networks:
  holonauts-network:
    name: holonauts-network
    external: true
    driver: bridge

volumes:
  medical-app_studies-data:
    external: true
